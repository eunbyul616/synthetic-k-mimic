defaults:
  - model@model: tabular_ehr_gen
  - train: tabular_ehr_gen/static_ae
  - log: log
  - data@dataset: common
  - data@dataloader: loader
  - data@data: tabular_ehr_gen
  - data@manipulation: manipulation
  - preprocess@preprocess: k_mimic
  - common@path: path
  - common@ignore: ignore
  - evaluation: evaluation_tabular_ehr_gen

hydra:
  run:
    #    dir: /Absolute/Path/for/Project/log/hydra/${now:%Y-%m-%d}/${now:%H-%M}  # Change needed in front of the "/log"
    dir: /path/to/your/project/log/hydra/${now:%Y-%m-%d}/${now:%H-%M}

device_num: 0
seed: 42